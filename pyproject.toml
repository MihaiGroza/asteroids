[project]
name = "asteroids"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pygame==2.6.1",
    "python-dotenv>=1.2.2",
    "requests>=2.32.5",
]

[tool.uv.workspace]
members = [
    "my_project",
    "asteroids",
]
